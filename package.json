{
  "private": true,
  "engines": {
    "node": "24"
  },
  "type": "module",
  "scripts": {
    "build": "nitro build",
    "dev": "nitro dev",
    "lint": "eslint --fix ./",
    "prepare": "nitro prepare",
    "preview": "node .output/server/index.mjs"
  },
  "devDependencies": {
    "@fimion/lint-config": "^1.0.1",
    "@netlify/blobs": "latest",
    "eslint": "^9.32.0",
    "nitropack": "latest",
    "prettier": "^3.6.2"
  }
}
